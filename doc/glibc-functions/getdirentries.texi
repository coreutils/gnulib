@node getdirentries
@subsection @code{getdirentries}
@findex getdirentries

Gnulib module: ---

Portability problems fixed by Gnulib:
@itemize
@end itemize

Portability problems not fixed by Gnulib:
@itemize
@item
This function is missing on some platforms:
Minix 3.1.8, AIX 4.3.2, IRIX 6.5, Solaris 11.4, Cygwin, mingw, MSVC 14, Interix 3.5, BeOS, Android 9.0.
@item
On platforms where @code{off_t} is a 32-bit type, this function may not
work correctly on huge directories larger than 2 GB.  The fix is to use
the @code{AC_SYS_LARGEFILE} macro.
@end itemize
